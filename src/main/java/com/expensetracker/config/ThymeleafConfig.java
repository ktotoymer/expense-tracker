package com.expensetracker.config;

import org.springframework.context.annotation.Configuration;

/**
 * Конфигурация Thymeleaf (если нужна).
 * MoneyFormatter используется как статический утилитный класс через T() в шаблонах.
 */
@Configuration
public class ThymeleafConfig {
    // Пустой класс, MoneyFormatter используется как статический утилитный класс
}

