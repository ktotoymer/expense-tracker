# Инструкция по загрузке проекта на GitHub

## ✅ Что уже сделано:

1. ✅ Защищен `application.properties` (добавлен в `.gitignore`)
2. ✅ Создан шаблон `application.properties.example`
3. ✅ Создан `README.md`
4. ✅ Создан скрипт автоматизации `deploy-to-github.sh`

## 🚀 Быстрая загрузка (автоматический способ):

### Шаг 1: Создайте репозиторий на GitHub

1. Откройте https://github.com/new
2. Введите название репозитория (например: `expense-tracker`)
3. Выберите **Private** или **Public**
4. **НЕ** добавляйте README, .gitignore или лицензию
5. Нажмите "Create repository"

### Шаг 2: Выполните скрипт

Откройте терминал в папке проекта и выполните:

```bash
# Сделайте скрипт исполняемым
chmod +x deploy-to-github.sh

# Запустите скрипт (замените YOUR_USERNAME и REPO_NAME)
./deploy-to-github.sh YOUR_USERNAME REPO_NAME

# Пример:
./deploy-to-github.sh mikhailalexeev expense-tracker
```

Скрипт автоматически:
- Проверит статус git
- Добавит все файлы
- Создаст коммит
- Настроит удаленный репозиторий
- Загрузит код на GitHub

---

## 📝 Ручная загрузка (пошагово):

Если предпочитаете выполнить команды вручную:

### 1. Проверьте статус:
```bash
git status
```

### 2. Добавьте все файлы:
```bash
git add .
```

### 3. Создайте коммит:
```bash
git commit -m "Initial commit: Expense Tracker application"
```

### 4. Создайте репозиторий на GitHub:
- Откройте https://github.com/new
- Создайте репозиторий (без README)

### 5. Добавьте remote и загрузите:
```bash
# Добавьте удаленный репозиторий (замените YOUR_USERNAME и REPO_NAME)
git remote add origin https://github.com/YOUR_USERNAME/REPO_NAME.git

# Переименуйте ветку в main (если нужно)
git branch -M main

# Загрузите код
git push -u origin main
```

---

## 🔒 Важно: Защита данных

Файл `application.properties` с паролями и API ключами **НЕ будет загружен** на GitHub (добавлен в `.gitignore`).

Для других разработчиков:
1. Скопируйте `application.properties.example` в `application.properties`
2. Заполните своими значениями

---

## ❓ Проблемы?

### Ошибка: "remote origin already exists"
```bash
# Удалите существующий remote
git remote remove origin

# Добавьте заново
git remote add origin https://github.com/YOUR_USERNAME/REPO_NAME.git
```

### Ошибка: "Authentication failed"
Настройте git credentials:
```bash
git config --global user.name "Your Name"
git config --global user.email "your.email@example.com"
```

Для HTTPS может потребоваться Personal Access Token вместо пароля.

### Ошибка: "Repository not found"
Убедитесь, что:
- Репозиторий создан на GitHub
- Название репозитория указано правильно
- У вас есть права на запись

---

## ✅ После успешной загрузки:

Проект будет доступен по адресу:
```
https://github.com/YOUR_USERNAME/REPO_NAME
```
